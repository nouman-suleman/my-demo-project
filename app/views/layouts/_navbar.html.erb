<nav class="navbar navbar-expand-lg navbar-light bg-light">
  <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent"
    aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
    <span class="navbar-toggler-icon"></span>
  </button>

  <div class="collapse navbar-collapse" id="navbarSupportedContent">
    <ul class="navbar-nav mr-auto">
      <li class="nav-item active">
        <a class="nav-link" href="/">Home <span class="sr-only">(current)</span></a>
      </li>
      <li class="nav-item acitve">
        <% if current_user %>
          <%= link_to "Cart", user_cart_path(current_user), :class => "nav-link" %><span class="sr-only">(current)</span>
        <% else %>
          <%= link_to "Cart", new_user_session_path, :class => "nav-link" %><span class="sr-only">(current)</span>
        <% end %>
      </li>
      <li class="nav-item active">
        <a class="nav-link" href="#">Check Out <span class="sr-only">(current)</span></a>
      </li>
    </ul>
    <form class="form-inline my-2 my-lg-0">
      <input class="form-control mr-sm-2" type="search" placeholder="Search" aria-label="Search">
      <button class="btn btn-outline-primary my-2 my-sm-0" type="submit">Search</button>
    </form>
    <form class="form-inline my-2 my-lg-0">
      <span class="navbar-text">
          <% if user_signed_in? %>
            <%= current_user.name.titleize %>
            <%= link_to "Sign Out", destroy_user_session_path, method: :delete, class: "btn btn-success" %>
          <% else %>
            <ul class="navbar-nav mr-auto">
              <li class="nav-item active mr-auto">
                <%= link_to "Sign Up", new_user_registration_path, class: "nav-link active"  %>
              </li>
              <li class="nav-item active mr-auto">
                <%= link_to "Sign In", new_user_session_path, class: "nav-link active" %>
              </li>
            </ul>
          <% end %>
        </span>
    </form>
  </div>
</nav>
